# Структура истории: что игра сама считает основным, и где у неё границы

Разведка 2026-08-06. Всё ниже добыто из установленной игры
(`G:\SteamLibrary\steamapps\common\Baldurs Gate 3\Data`) тем же `tools\lslib\Tools\Divine.exe`,
что и остальное. Ничего в игру не писалось, игра не запускалась.

Продолжение [data-mining-findings.md](data-mining-findings.md), раздел 1: там разобрано, как из
журнала получить «куда идти» для одного шага. Здесь — то, что лежит уровнем выше: **как игра
делит свои 167 квестов на сюжет и не-сюжет, как она размечает акты, и где именно контент
теряется навсегда.**

Документ намеренно доведён до конца акта 1 и не дальше: акты 2 и 3 названы только категориями и
числами. Внутри акта 1 названы места, которые сканер и так перечисляет игроку.

## 1. У игры есть собственная таблица «основное / побочное»

`Gustav.pak → Mods/GustavDev/Story/Journal/questcategory_prototypes.lsx` — **14 категорий**, у
каждой `CategoryID`, переведённое имя и `SortingPriority`. Это разделы журнала, в порядке показа:

| приоритет | CategoryID | имя | квестов |
|---|---|---|---|
| 10 | `MainQuest` | Основное задание | 31 |
| 10 | `Tutorial` | Пролог | 1 |
| 9 | `PersonalStory` | Личное задание | 26 |
| 8 | `BaldursGate` | Врата Балдура | 29 |
| 7 | `Shadowcurse` | Оскверненные тенью земли | 13 |
| 6 | `Creche` | Ясли гитьянки | 3 |
| 5 | `Grove` | Роща друидов | 14 |
| 4 | `Underdark` | Подземье | 13 |
| 3 | `Crashside` | Район крушения наутилоида | 14 |
| 1 | `Companions` | Спутники | 15 |
| 0 | `Act1_HiddenBoosters` | — | 4 |
| 0 | `Act2_HiddenBoosters` | — | 2 |
| 0 | `Act3_HiddenBoosters` | — | 2 |
| 0 | `CompletedQuests` | Завершенные задания | — |

Каждый узел `Quest` в `quest_prototypes.lsx` несёт `CategoryID`, свой `SortingPriority`,
`ParentQuestID` и `QuestVisiblity`. **158 квестов видимых, 9 служебных** (`HIDDEN_*_Boosters`,
`QuestVisiblity=false`) — их слой не должен озвучивать никогда.

Три категории — не жанр, а география: `Grove`, `Underdark`, `Crashside` (плюс `Creche`) — это
акт 1, `Shadowcurse` — акт 2, `BaldursGate` — акт 3. То есть **побочек акта 1 — 44 штуки**,
а основных квестов в нём — два.

**Оговорка, которая стоит проверки перед использованием:** категория — это где нить
*открывается*, а не где она проходит. `SHA_Nightsong` и `PLA_GrandDukeRescue` лежат в
`Crashside`, а большинство их маркеров — в `CTY_Main_A`, то есть в акте 3. Для «в каком я акте»
категория не годится; для «это сюжет или нет» — годится.

## 2. Приоритет внутри `MainQuest` — это порядок истории

| prio | QuestID | что это |
|---|---|---|
| 101 | `TUT_NautiloidEscape` | пролог |
| 102 | `GLO_Tadpole` | **весь акт 1** |
| 105 | `GLO_PathToMoonrise` | переход в акт 2 |
| 110, 109 | `GLO_Moonrise`, `MOO_EndKetheric` | акт 2 |
| 150 | `GLO_GatherYourAllies` | между актами |
| 210–212 | `LOW_*`, `WYR_*`, `GLO_DealWithTheBrain` | акт 3 |

Порядок почти монотонный (110/109 — единственная перестановка). Всё, что ниже 10, — подквесты,
и они висят на родителе через `ParentQuestID`.

**Акт 1 в основной линии — это хаб, а не коридор.** У `GLO_Tadpole` шесть подквестов
`GLO_Tadpole_SUB_*`, и это не список дел, а **шесть альтернативных зацепок**: разные люди в
разных районах, каждый обещает своё решение. У `GLO_PathToMoonrise` — два подквеста-маршрута
(`SUB_UnderdarkPath`, `SUB_MountainPath`), оба ведут в акт 2.

Практическое следствие для слоя: на вопрос «куда идти по сюжету» правильный ответ в акте 1 —
не одна точка, а несколько равноправных, и их надо отдавать списком.

## 3. Места размечены актами прямо в данных

Из [data-mining-findings.md §8](data-mining-findings.md) уже известно:
`DB_WaypointInfo("Act1", "WAYP_GOB_Temple", (ITEM)святилище, (TRIGGER)точка)`. **Первый аргумент —
группа акта**, и это готовый тег для каждой точки перехода:

| группа | точек |
|---|---|
| `Act1` | 16 |
| `Act1b` | 3 |
| `Act2` | 8 |
| `Act3` | 3 |
| `Act3b` | 10 |

Уровни делятся так же однозначно: `WLD_Main_A` и `CRE_Main_A` — акт 1, `SCL_Main_A` — акт 2,
`BGO_Main_A` / `CTY_Main_A` / `IRN_Main_A` — акт 3, `TUT_Avernus_C` — пролог, `END_Main` и
`EPI_Main_A` — финал.

Областей на уровень (из `lua/a11y-placedata.lua`): `CTY_Main_A` 110, **`WLD_Main_A` 52**,
`BGO_Main_A` 31, `SCL_Main_A` 23, `CRE_Main_A` 9. То есть 268 областей всей игры игрок никогда
не видит разом — в акте 1 сканеру есть что показать ровно из пятидесяти двух.

### Акт 1 по префиксам

Префикс области — та же авторская группировка, что и в идентификаторах квестов, и её достаточно,
чтобы полтинник строк стал восемью группами:

| префикс | мест | что это |
|---|---|---|
| `DEN` | 14 | Изумрудная роща и всё внутри: покои друидов, убежище тифлингов, хранилище, подземный ход |
| `UND` | 14 | Подземье: Эбенозеро, Гримфордж, колония миконидов, волшебная башня, адамантиновая кузня |
| `HAG` | 6 | болота и логово под ними |
| `FOR` | 5 | лес, вымершая деревня, Шепчущая бездна |
| `GOB` | 5 | лагерь гоблинов, оскверненный храм, загоны варгов |
| `PLA` | 4 | Всхожая дорога, Приют Вокин, убежище Зентарим, горный перевал |
| `CHA` | 3 | заросшие руины: вход, трапезная, склеп |
| `CRA` | 1 | место крушения |

Те же три буквы стоят в `QuestID` (`DEN_Conflict`, `UND_CaveIn`, `GOB_Misc`), так что связь
«место → какие тут задания» — это сравнение префикса, без всякой геометрии.

## 4. Границы актов: один флаг и пятнадцать вопросов «вы уверены»

Из `Mods/*/Story/RawFiles/Goals/`. Это самая полезная часть разведки.

**Флаг** `Act2_PointOfNoReturnReached_a3155f30-b8f3-4db5-ac21-d3036f4426e3`.

Пока он не поставлен, **из акта 2 можно вернуться в акт 1**. В `Act2_General.txt` для этого есть
всё: два телепорта обратно (`S_SCL_LevelSwapTeleporter_Mountain`,
`S_SCL_Elevator_Fort_ToUnderdark`), запрос `QRY_SCL_General_CanReturnToAct1()` и триггеры-области
с диалогом-предупреждением на подходе (`S_SCL_Act1ReturnAttemptDialogBox_*`). Расхожее «вошёл в
акт 2 — назад дороги нет» в данных неверно: дорога есть, и какое-то время.

Флаг ставится позже, уже внутри акта 2, и только после явного вопроса игроку:

```
IF ReadyCheckPassed("ReadyCheck_EnterNightsongPrison")
THEN PROC_SCL_General_BlockReturnToAct1();

PROC PROC_SCL_General_BlockReturnToAct1()
THEN
PROC_LockAllGroupWaypoints("Act1");
PROC_LockAllGroupWaypoints("Act1b");
DB_SCL_Teleporters_Blocked(...);
```

Что происходит в этот момент:

- закрываются **все 19 точек перехода групп `Act1` и `Act1b`**;
- **12 квестов акта 1 закрываются принудительно** — `CHA_Chapel`, `FOR_IncompleteMasterwork`,
  `HAG_HagSpawn`, `PLA_StuckHalfElf`, `UND_AdamantineForge`, `UND_BroodingSovereign`,
  `UND_CaveIn`, `UND_DuergarPoison`, `UND_GnomeRescue`, `UND_MyconidCircle`, `UND_MyconidRevenge`
  и сам `GLO_Tadpole`. Состояния называются прямо: `LeftAct`, `ReachedNoReturn`, `Left`,
  `LeftActRubble`. Незакрытые зацепки основного квеста получают шаг `Tadpole_PointOfNoReturn`.

**`ReadyCheck…` — это собственный список игры «вы уверены».** Их около пятнадцати на всю игру, и
это исчерпывающий перечень настоящих точек невозврата: один ещё в акте 1 (у яслей гитьянки), два
в акте 2, переход из акта 2 в акт 3 (`ReadyCheck_ToBGFromSCL`), остальные — в акте 3 и финале.

Отсюда два вывода, которые и надо доносить игроку:

1. **Порядком действий сценарий не ломается.** Всё висит на флагах, не на времени, и границу
   акта игра проходит с уборкой, а не с висящими нитями.
2. **Контент теряется только на границах, и игра всегда спрашивает первой.** Слой, который эти
   вопросы читает и объясняет их цену, спасает прохождение целиком.

## 5. Темп: отдых, уровни, отсутствие таймеров

Единственный раздел, где данные не первоисточник, — здесь опрошены гайды, ссылки внизу.

- **Глобальных таймеров нет, основную линию провалить нельзя** — так сделано намеренно. Рискуют
  побочки, сцены спутников и жизни отдельных NPC.
- **Долгий отдых — главный клапан темпа.** Он двигает лагерные сцены и линии спутников, а не
  квесты. За акт 1 нормально уйти на 8–12 долгих отдыхов; кто не отдыхает — не видит половину
  персонажей. Если NPC говорит «до завтра» — он про следующий долгий отдых, и это единственный
  таймер в игре.
- **Уровни:** акт 1 проходится до 5–7, в акт 2 не ниже 5 (комфортно 6–7), в акт 3 — 8–10,
  потолок 12. Сильное отставание значит пропущенный район, а не отставание по сюжету.

## 6. Что из этого уже можно сделать в слое

Половина лежит в собранных индексах и не используется.

- **`M.q[questID] = { titleHandle, categoryID }` в `lua/a11y-questdata.lua` уже есть.** Цепочка
  `descHandle → M.oh → objectiveID → questID → categoryID` замкнута, значит к каждой прочитанной
  цели можно добавлять «основное задание» / «побочное» / «спутник» — одна строка в озвучке, и
  главный вопрос игрока («это сюжет или я гуляю?») закрыт.
- **Разбить категорию «квест» по разделам журнала** и сортировать по `SortingPriority` — это
  порядок самой игры, а не наш выдуманный.
- **Проставить акт каждому месту**: по группе точки перехода и по уровню (§3). Дальше — не
  показывать в списке то, что к текущему акту не относится, и говорить «эта точка перехода
  закроется вместе с актом».
- **Группировать «локации» по префиксу** (§3): «Изумрудная роща: 14 мест, в двух есть активные
  цели» вместо пятидесяти строк подряд.
- **Ловить `ReadyCheck` и предупреждать вслух.** Зрячий видит модальное окно «это точка
  невозврата»; в контроллерном интерфейсе его легко пролететь. Предупреждение с ценой перехода
  («закроются 19 точек перехода и незавершённые задания акта 1») — функция, которой нет ни у
  кого, и она прямо спасает прохождение.
- **«Куда идти по сюжету»** — отдельный ответ, а не первая строка сканера: взять активные квесты
  категории `MainQuest`, их цели, маркеры, уровень и позицию. В акте 1 таких точек будет
  несколько, и это правильно (§2).

## Чего здесь нет

- ~~Как `ReadyCheck` виден изнутри слоя, не проверено.~~ **Проверено 2026-08-06 в живой игре:**
  `Ext.Osiris.RegisterListener("ReadyCheckGlobal", 4, "after", …)` регистрируется успешно, а
  тексты берутся по ключу сообщения из `Localization/ReadyChecks_Descriptions.lsf` (33 записи в
  `Shared.pak` и `Gustav.pak`, плюс 4 в соседних `Misc`). Собирается
  `tools/build-readycheck-index.ps1` → `lua/a11y-rcdata.lua`; слой произносит слова самой игры.
- `PROC_LockAllGroupWaypoints` разобран по имени группы, но **список групп собран из вызовов, а
  не из таблицы** — если в игре есть группа, которую никто не блокирует, её здесь не видно.
- Акты 2 и 3 сознательно не разбирались дальше границ и чисел.
- Категории `*_HiddenBoosters` не вскрывались: судя по именам, это раздача бонусов, а не сюжет.
